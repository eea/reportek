import Vue from 'vue'
import EnvelopesWIP from '@/js/components/EnvelopesWIP'

describe('EnvelopesWIP.vue', () => {
  // Inspect the raw component options
  it('has a created hook', () => {
    expect(EnvelopesWIP.created).to.be.a('function')
  })
})
